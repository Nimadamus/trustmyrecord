# indexnow-state

Written only by `.github/workflows/indexnow.yml` on main.

* `state.json`: last processed deployed commit, pending URLs, recent submissions (dedupe)
* `log.jsonl`: one JSON line per run: timestamp, URLs, URL count, HTTP status and body, skips

Not a site branch. GitHub Pages serves main only.
