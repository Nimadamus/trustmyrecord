name: Account Integrity Live Proof

on:
  workflow_dispatch:
  push:
    paths:
      - 'tests/account-integrity-live-browser-proof.js'
      - '.github/workflows/account-integrity-live-proof.yml'

jobs:
  profile-live-browser-proof:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
      - run: npm install
      - run: npx playwright install --with-deps chromium
      - run: sudo apt-get update && sudo apt-get install -y imagemagick
      - run: xvfb-run --auto-servernum node tests/account-integrity-live-browser-proof.js
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: account-integrity-live-proof
          path: |
            artifacts/account-integrity-deleted-profile-live-browser-proof.png
            artifacts/account-integrity-live-proof.json
